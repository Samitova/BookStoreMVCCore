@model UserClaimsViewModel
@{
    ViewData["Title"] = "Manage user claims";
    var userId = ViewBag.UserId;
}

<div class="container">
    <form asp-action="ManageUserClaims" asp-controller="Administration" method="post">
        <input type="hidden" asp-for="@userId" />
        <div class="card">
            <div class="card-header text-center">
                <h5>Manage user claims</h5>
            </div>
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="card-body">
                @for (int i = 0; i < Model.Claims.Count; i++)
                {
                    <div class="form-check m-1">
                        <input type="hidden" asp-for="@Model.Claims[i].ClaimType" />                      

                        <input class="form-check-input" asp-for="@Model.Claims[i].IsSelected" />
                        <label class="form-check-label" asp-for="@Model.Claims[i].IsSelected">
                            @Model.Claims[i].ClaimType
                        </label>
                    </div>
                }
            </div>
            <div class="card-footer">
                <input type="submit" value="Update" class="btn edit-btn" />
                <a class="btn return-btn" asp-controller="Administration" asp-action="EditUser" asp-route-id="@userId">Cancel</a>
            </div>
        </div>
    </form>
</div>