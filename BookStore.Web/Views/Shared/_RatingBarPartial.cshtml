@model double
<div class="comment-rate">
    @for (int i = 0; i < Model; i++)
    {
        <i class="fa-solid fa-star"></i>
    }
    @for (int i = 4; i >= Model; i--)
    {
        <i class="fa-solid fa-star notchecked"></i>
    }
</div>